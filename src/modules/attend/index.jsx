import AttendComponent from "../../components/attend";

const Attend = ({ userAttended, setUserAttended }) => <AttendComponent
    userAttended={userAttended} 
    setUserAttended={setUserAttended}
/>

export default Attend